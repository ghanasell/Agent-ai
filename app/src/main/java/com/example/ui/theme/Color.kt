package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium IDE Terminal Palette
val CarbonDark = Color(0xFF0F0F11)      // Main background
val SlateSurface = Color(0xFF1E1E24)     // Card / sheet surface
val CharcoalVariant = Color(0xFF2A2A32)  // Code-input and dark containers

val NeonCyan = Color(0xFF00E5FF)         // Primary / accent glows (DeepSeek branding style)
val TerminalGreen = Color(0xFF00E676)    // Success / secondary actions
val ElectricAmber = Color(0xFFFFA000)    // Warnings / highlights

val TextPrimary = Color(0xFFF3F4F6)      // Bright white for high readability
val TextSecondary = Color(0xFF9CA3AF)    // Muted grey for auxiliary labels
val CodeBorder = Color(0xFF374151)       // Separator borders
